<div id="calendar">
  <%= calendar do |date| %>
    <%= date.day %>
    <% if @entries_by_date[date] %>
       <ul>
      <% @entries_by_date[date].each do |entry| %>
        <li><%= link_to entry.message_type, entry %></li>
      <% end %>
    </ul>
    <% end %>
  <% end %>
</div>

