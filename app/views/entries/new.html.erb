<% title "New Entry" %>

<% if :type == 'snapshot' %>
<%= render 'snapshot_form' %>
<% elsif :type == 'happening' %>
<%= render 'happening_form' %>
<% elsif :type == 'reflection' %>
<%= render 'reflection_form' %>
   


<p><%= link_to "Back to List", entries_path %></p>
